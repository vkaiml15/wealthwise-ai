version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Build phase - minimal operations only"
run:
  runtime-version: 3.11
  pre-run:
    - pip3 install --no-cache-dir -r requirements.txt
  command: python3 strand_main.py
  network:
    port: 8000
